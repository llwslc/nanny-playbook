# ─────────────────────────────────────────────────────────
# 这是一个公开仓库。只放指南，不放任何个人信息。
#
# 候选人材料（身份证、征信报告、体检报告、无犯罪记录）
# 请存放在这个仓库【之外】的目录。
#
# 下面是纵深防御——万一手滑，也进不了 git。
# git 历史是永久的：提交一次，删掉也还在历史里和每一个 clone 里。
# ─────────────────────────────────────────────────────────

# 你填好的私人版本（岗位定义、评分表、记录表）放这里
本地/
local/

# 扫描件 / 照片 / 报告，一律不进 git
*.pdf
*.jpg
*.jpeg
*.png
*.gif
*.heic
*.webp
*.zip
*.doc
*.docx
*.xls
*.xlsx

# 按文件名兜底（扫描件、笔记）
*身份证*
*征信*
*体检*
*无犯罪*
*背调*

# 但指南本身的 md 必须能提交——上面的规则会误伤 流程/3-背调.md 这类文件名。
# 本地/ 已整个排除，其下的 md 不会被这条反选救回来（父目录被排除就无法再包含）。
!*.md

# OS
.DS_Store
Thumbs.db
